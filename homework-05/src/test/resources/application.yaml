spring:
  datasource:
    url: jdbc:mysql://localhost/library_test?serverTimezone=Europe/Moscow
    username: otus
    password: javaclub
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
    schema: schema.sql
    data: data.sql